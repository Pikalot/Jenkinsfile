/* Requires the Docker Pipeline plugin */

}
pipeline {
    agent any
    stages {
        stage("Good Day") {
            steps {
                echo "Welcome to Jenkins!"
            }
        }
    }
}
